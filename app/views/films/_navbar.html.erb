<header class="w-full flex bg-gradient-to-r from-purple-500 to-pink-500 opacity-95 shadow-md fixed top-0 h-10">
  <div class="container mx-auto flex flex-row gap-2 justify-end items-center">
    <% if user_signed_in? %>
      <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
      <%= link_to "My Favs", my_favs_path %>
    <% else %>
      <h5><%= button_to "Inicia Sesión", new_user_session_path, method: "GET", class: "px-2 rounded cursor-pointer transition easy-in-out bg-blue-300 hover:bg-red-300" %></h5>
    <% end %>
    <%= render "/films/search_form" %>
  </div>
</header>